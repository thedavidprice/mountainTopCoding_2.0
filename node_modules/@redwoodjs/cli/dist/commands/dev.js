"use strict";

var _Object$defineProperty = require("@babel/runtime-corejs3/core-js/object/define-property");

var _interopRequireDefault = require("@babel/runtime-corejs3/helpers/interopRequireDefault").default;

_Object$defineProperty(exports, "__esModule", {
  value: true
});

exports.handler = exports.builder = exports.description = exports.command = void 0;

var _includes = _interopRequireDefault(require("@babel/runtime-corejs3/core-js/instance/includes"));

var _filter = _interopRequireDefault(require("@babel/runtime-corejs3/core-js/instance/filter"));

var _map = _interopRequireDefault(require("@babel/runtime-corejs3/core-js/instance/map"));

var _keys = _interopRequireDefault(require("@babel/runtime-corejs3/core-js/object/keys"));

var _fs = _interopRequireDefault(require("fs"));

var _path = _interopRequireDefault(require("path"));

var _concurrently = _interopRequireDefault(require("concurrently"));

var _terminalLink = _interopRequireDefault(require("terminal-link"));

var _internal = require("@redwoodjs/internal");

var _lib = require("../lib");

var _colors = _interopRequireDefault(require("../lib/colors"));

var _generatePrismaClient = require("../lib/generatePrismaClient");

var _runPreBuildTasks = _interopRequireDefault(require("../lib/runPreBuildTasks"));

const command = 'dev [side..]';
exports.command = command;
const description = 'Start development servers for api, and web';
exports.description = description;

const builder = yargs => {
  yargs.positional('side', {
    choices: ['api', 'web'],
    default: ['api', 'web'],
    description: 'Which dev server(s) to start',
    type: 'array'
  }).positional('forward', {
    alias: 'fwd',
    description: 'String of one or more Webpack DevServer config options, for example: `--fwd="--port=1234 --open=false"`',
    type: 'string'
  }).option('esbuild', {
    type: 'boolean',
    required: false,
    description: 'Use ESBuild for api side [experimental]'
  }).epilogue(`Also see the ${(0, _terminalLink.default)('Redwood CLI Reference', 'https://redwoodjs.com/reference/command-line-interface#dev')}`);
};

exports.builder = builder;

const handler = async ({
  side = ['api', 'web'],
  forward = '',
  esbuild = false
}) => {
  var _context, _context2;

  // We use BASE_DIR when we need to effectively set the working dir
  const BASE_DIR = (0, _lib.getPaths)().base; // For validation, e.g. dirExists?, we use these
  // note: getPaths().web|api.base returns undefined on Windows

  const API_DIR_SRC = (0, _lib.getPaths)().api.src;
  const WEB_DIR_SRC = (0, _lib.getPaths)().web.src; // Run tasks like type generate, etc.

  (0, _runPreBuildTasks.default)();

  if ((0, _includes.default)(side).call(side, 'api')) {
    try {
      await (0, _generatePrismaClient.generatePrismaClient)({
        verbose: false,
        force: false,
        schema: (0, _lib.getPaths)().api.dbSchema
      });
    } catch (e) {
      console.error(_colors.default.error(e.message));
    }

    try {
      await (0, _internal.shutdownPort)((0, _internal.getConfig)().api.port);
    } catch (e) {
      console.error(`Error whilst shutting down "api" port: ${_colors.default.error(e.message)}`);
    }
  }

  if ((0, _includes.default)(side).call(side, 'web')) {
    try {
      await (0, _internal.shutdownPort)((0, _internal.getConfig)().web.port);
    } catch (e) {
      console.error(`Error whilst shutting down "web" port: ${_colors.default.error(e.message)}`);
    }
  }

  const jobs = {
    api: {
      name: 'api',
      command: `cd "${_path.default.join(BASE_DIR, 'api')}" && yarn cross-env NODE_ENV=development yarn dev-server`,
      prefixColor: 'cyan',
      runWhen: () => _fs.default.existsSync(API_DIR_SRC)
    },
    web: {
      name: 'web',
      command: `cd "${_path.default.join(BASE_DIR, 'web')}" && yarn webpack-dev-server --config ../node_modules/@redwoodjs/core/config/webpack.development.js ${forward}`,
      prefixColor: 'blue',
      runWhen: () => _fs.default.existsSync(WEB_DIR_SRC)
    }
  };

  if (esbuild) {
    jobs.api.name = 'api esbuild';
    jobs.api.command = 'yarn cross-env NODE_ENV=development NODE_OPTIONS=--enable-source-maps yarn rw-api-server-watch';
    jobs.web.name = 'web esbuild';
    jobs.web.command = 'yarn cross-env ESBUILD=1 && ' + jobs.web.command;
  }

  (0, _concurrently.default)((0, _filter.default)(_context = (0, _map.default)(_context2 = (0, _keys.default)(jobs)).call(_context2, n => (0, _includes.default)(side).call(side, n) && jobs[n])).call(_context, job => job && job.runWhen()), {
    prefix: '{name} |',
    timestampFormat: 'HH:mm:ss'
  }).catch(e => {
    if (typeof (e === null || e === void 0 ? void 0 : e.message) !== 'undefined') {
      console.log(_colors.default.error(e.message));
    }
  });
};

exports.handler = handler;